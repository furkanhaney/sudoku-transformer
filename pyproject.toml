[project]
name = "sudoku"
version = "0.1.0"
description = "A project to train transformers on sudoku."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.0.0",
    "pandas>=2.2.0",
    "torch>=2.5.0",
    "tqdm>=4.66.0",
    "torchmetrics>=1.5.0",
    "pydantic>=2.0.0",
    "ipykernel>=7.1.0",
    "matplotlib>=3.10.7",
]
